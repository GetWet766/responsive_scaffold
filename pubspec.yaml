name: responsive_scaffold
description: A responsive scaffold package following Material 3 adaptive layout specifications.
version: 0.1.0

environment:
  sdk: ^3.7.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
